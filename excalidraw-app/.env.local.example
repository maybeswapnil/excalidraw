# Remote sync configuration (optional)
# Enable syncing canvas state to MongoDB backend
VITE_ENABLE_REMOTE_SYNC=true

# Backend endpoint URL (must match the running backend server)
VITE_REMOTE_SYNC_ENDPOINT=http://192.168.1.100:5001
